# analyse.py n'utilise que la bibliothèque standard de Python (>= 3.10).
# Aucune dépendance externe : ce fichier reste volontairement vide.
# Présent pour que l'étape `pip install -r analysis/requirements.txt` de la CI réussisse.
